<div class="tab-pane" id="Article-{{this._id}}"> 
    <img class="card-img-top" src="{{this.img}}"></img>
    <div class="h4 card-title mt-3">{{this.title}}"</div>
    <div class="card-text">{{this.description}}"</div> 
</div> 
<div class="tab-pane p-3 active" id="comment-{{this._id}}"> 
    <div class="card">
        <div class="card-body" id="card-body-{{this.comment._id}}">
            <div class="card-text">
                {{#if this.comment.comment}}     
                    <p>{{this.comment.comment}}</p>
                {{else}}
                    <p>No comments for this Article</p>
                {{/if}}
            </div>
        {{#if this.comment.comment}}        
            <button class="btn btn-warning card-link btnEditComment" 
                data-toggle="modal" 
                data-id="{{this._id}}" 
                data-target="#editCommentModal">Edit
            </button>
            <button class="btn btn-secondary card-link btnDelComment" 
                data-id="{{this.comment._id}}">Del
            </button>
        {{else}}        
            <button class="btn btn-warning card-link btnEditComment" 
                data-toggle="modal" 
                data-id="{{this._id}}" 
                data-target="#editCommentModal">Add
            </button>
        {{/if}}
        </div>
    </div>
</div>